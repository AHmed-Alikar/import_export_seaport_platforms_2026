# Copy to .env and fill in real values. .env is gitignored — an earlier
# version of this file was accidentally committed with real-looking
# credentials; see the security note in the repo README.

SECRET_KEY=
DATABASE_URI=mysql://user:password@localhost/seaport_db
